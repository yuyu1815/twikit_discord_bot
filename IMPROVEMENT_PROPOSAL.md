# プロジェクト改善提案書：コードレビューとデータベース移行

このドキュメントは、`src`ディレクトリ内のコードレビュー結果と、データ永続化戦略をJSONファイルからSQLiteデータベースへ移行するための具体的な計画をまとめたものです。

---

## 1. コードレビューに基づく改善点

リファクタリング後のコードは構造化されていますが、さらに堅牢性と効率性を高めるための改善点がいくつか見つかりました。

### 1.1. `core/bot.py` - グローバルなクールダウンタイマーの問題
- **現状**: すべてのサーバー（ギルド）で単一のタイマー (`self.old_time`) を共有しています。
- **問題点**: 1つのサーバーでツイートをチェックすると、他のすべてのサーバーも同じクールダウン時間待機する必要があり、非効率です。
- **改善案**: サーバーごとに最終チェック時間をデータベースに記録し、個別にクールダウンを管理します。

### 1.2. `cogs/twitter_commands.py` - 設定更新の競合と非効率性
- **現状**: 設定を変更する際、「JSON全体を読み込み → 一部をメモリで変更 → JSON全体を書き込む」という手順を踏んでいます。
- **問題点**: 複数のコマンドが同時に実行されると、変更が上書きされる競合状態（レースコンディション）が発生する可能性があります。
- **改善案**: データベースに移行し、トランザクションを利用してアトミックな（分割不可能な）更新処理を行います。これにより、`add_twitter_feed`のような単一の操作で安全にデータを変更できます。

### 1.3. `core/twitter_client.py` - 広範な例外処理と再帰の問題
- **現状**: `except Exception:` で全てのエラーを画一的に処理しています。また、引用ツイートのURL取得が再帰呼び出しになっています。
- **問題点**: ユーザーが見つからない等の正常なエラーと、予期せぬエラーの区別がつかず、デバッグが困難です。また、深い引用チェーンでパフォーマンスが低下する可能性があります。
- **改善案**: `twikit.errors.UserNotFound` のように、ライブラリが提供する具体的な例外を捕捉します。再帰には深度制限を設け、無限ループを防ぎます。

### 1.4. `cogs/url_fixer.py` - URL置換ロジックの硬直性
- **現状**: メッセージ内の最初のURLしか処理していません。
- **問題点**: 1つのメッセージに複数のTwitterリンクが含まれている場合、2つ目以降が無視されます。
- **改善案**: メッセージ内のすべてのURLをループでチェックし、置換対象であればすべて処理するように修正します。

---

## 2. データ永続化戦略の刷新：SQLiteへの移行

現在のJSONによるデータ管理を、Python標準ライブラリの`sqlite3`を用いたデータベース管理に移行します。

### 2.1. データベース化のメリット
- **競合の排除**: データベースが同時アクセスを適切に管理し、設定の上書きや破損を防ぎます。
- **パフォーマンス向上**: 大量のデータを扱う場合でも、インデックスを用いて高速に必要な情報だけを取得できます。
- **データ整合性**: トランザクションにより、一連の操作がすべて成功するか、すべて失敗するかのどちらかになり、中途半端な状態でデータが保存されるのを防ぎます。
- **クエリの柔軟性**: 複雑な条件でのデータ取得（例：「このユーザーが登録されている全チャンネル」）が容易になります。

### 2.2. データベーススキーマ設計案

現在の `DiscordSetting.json` と `Twitter_msg.json` を置き換えるため、以下の3つのテーブルを設計します。

**1. `guild_settings` テーブル**
- ギルドごとの全体設定を管理

| カラム名 | 型 | 説明 |
| :--- | :--- | :--- |
| `guild_id` | INTEGER | DiscordギルドID (主キー) |
| `twitter_updates_enabled` | BOOLEAN | 新規ツイート通知の有効/無効 (デフォルト: TRUE) |
| `url_preview_enabled` | BOOLEAN | URLプレビュー置換の有効/無効 (デフォルト: TRUE) |
| `cool_down_minutes` | INTEGER | ツイートチェックのクールダウン時間（分） (デフォルト: 1) |
| `last_checked_time` | INTEGER | 最終チェック時間のUnixタイムスタンプ (デフォルト: 0) |

**2. `twitter_feeds` テーブル**
- どのチャンネルでどのユーザーを監視するかを管理

| カラム名 | 型 | 説明 |
| :--- | :--- | :--- |
| `id` | INTEGER | 自動採番されるID (主キー) |
| `guild_id` | INTEGER | DiscordギルドID |
| `channel_id` | INTEGER | DiscordチャンネルID |
| `twitter_user_name` | TEXT | 監視対象のTwitterユーザー名 |

**3. `tweet_history` テーブル**
- 最後に送信したツイートIDを記録

| カラム名 | 型 | 説明 |
| :--- | :--- | :--- |
| `feed_id` | INTEGER | `twitter_feeds`テーブルのID (主キー、外部キー) |
| `last_tweet_id` | INTEGER | 最後に処理したツイートのID |
| `second_last_tweet_id` | INTEGER | 2番目に新しかったツイートのID |

### 2.3. 必要なコード変更

1.  **`src/core/database.py` (新規作成)**
    - `sqlite3`に接続し、上記のテーブルを作成・管理する`Database`クラスを実装します。
    - `add_feed`, `remove_feed`, `get_guild_settings`, `update_last_tweet_id`など、すべてのSQL操作をこのクラスのメソッドとしてカプセル化します。

2.  **`src/config/settings.py` の役割変更**
    - このクラスは、ギルドごとの動的な設定管理（JSONの読み書き）から解放されます。
    - 主に言語ファイル (`lang/*.json`) のような、静的な設定の読み込みに責務を限定します。

3.  **`MyBot` と `Cogs` の修正**
    - `MyBot`クラス (`core/bot.py`) は、`Database`クラスのインスタンスを `self.db` のように保持します。
    - 各Cogは `self.bot.db` を通じてデータベースにアクセスします。
    - これまで `self.bot.settings.get_guild_config()` を呼び出していた部分は、`self.bot.db.get_guild_settings(guild_id)` のようなメソッド呼び出しに置き換わります。

## 3. まとめ

これらの変更（コードレビューの反映＋データベースへの移行）により、プロジェクトはより**安定的**で**効率的**、そして**スケーラブル**になります。特にデータベース化は、今後の機能拡張における強固な基盤となります。

次のステップとして、この計画に基づいた具体的な実装に着手することをお勧めします。